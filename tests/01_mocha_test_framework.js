/*
 * TODO: Fill in the `describe` and `it` statements.
 *
 * A `describe` statement should provide high-level context. An `it` statement
 * begins with 'should' and explains what the test does. Together, they should
 * give someone clear context on what the test is accomplishing.
 */

const assert = require('assert');

describe('', () => {
    it('', () => {
        assert.strictEqual(1 + 1, 2, '1 + 1 did not equal 2');
    });
});

describe('', () => {
    it('', () => {
        assert.strictEqual(1 - 1, 0, '1 - 1 did not equal 0');
    });
});

describe('', () => {
    it('', () => {
        assert.strictEqual(1 * 2, 2, '1 * 2 did not equal 2');
    });
});